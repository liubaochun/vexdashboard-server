import { applicationType } from '../../../helpers/application-type';
import { module, test } from 'qunit';

module('Unit | Helper | application type');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = applicationType(42);
  assert.ok(result);
});
