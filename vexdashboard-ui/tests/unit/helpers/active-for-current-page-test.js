import { activeForCurrentPage } from '../../../helpers/active-for-current-page';
import { module, test } from 'qunit';

module('Unit | Helper | active for current page');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = activeForCurrentPage(42);
  assert.ok(result);
});
